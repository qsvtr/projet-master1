# deploy smart contract ERC721 with truffle 

## Installation
`yarn install`

## Setup
`cd truffle`

`cp .env.example .env`

modify with your credentials

## Deploy contract with truffle
`cd truffle`

`truffle console --network development`

`migrate --network development`

## MintForm NFT
`node src/index.js`
